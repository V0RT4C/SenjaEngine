import { 
DEFAULT_LOOKBODY,
DEFAULT_LOOKFEET,
DEFAULT_LOOKHEAD,
DEFAULT_LOOKLEGS,
DEFAULT_LOOKTYPE,
  DEFAULT_PLAYER_SEX,
  DEFAULT_PLAYER_SPAWN_POSITION_X, 
  DEFAULT_PLAYER_SPAWN_POSITION_Y, 
  DEFAULT_PLAYER_SPAWN_POSITION_Z 
} from "~/config.ts";

export default function(){
 return `CREATE TABLE IF NOT EXISTS characters (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name VARCHAR(255) NOT NULL,
  group_id INTEGER NOT NULL DEFAULT 10,
  account_id INTEGER NOT NULL DEFAULT 0,
  world_id INTEGER DEFAULT NULL,
  level INTEGER NOT NULL DEFAULT 1,
  vocation INTEGER NOT NULL DEFAULT 0,
  health INTEGER NOT NULL DEFAULT 150,
  max_health INTEGER NOT NULL DEFAULT 150,
  experience UNSIGNED BIG INT NOT NULL DEFAULT 0,
  lookbody INTEGER NOT NULL DEFAULT ${DEFAULT_LOOKBODY},
  lookfeet INTEGER NOT NULL DEFAULT ${DEFAULT_LOOKFEET},
  lookhead INTEGER NOT NULL DEFAULT ${DEFAULT_LOOKHEAD},
  looklegs INTEGER NOT NULL DEFAULT ${DEFAULT_LOOKLEGS},
  looktype INTEGER NOT NULL DEFAULT ${DEFAULT_LOOKTYPE},
  magic_level INTEGER NOT NULL DEFAULT 0,
  mana INTEGER NOT NULL DEFAULT 0,
  max_mana INTEGER NOT NULL DEFAULT 0,
  mana_spent INTEGER UNSIGNED NOT NULL DEFAULT 0,
  soul_points INTEGER UNSIGNED NOT NULL DEFAULT 0,
  town_id INTEGER NOT NULL DEFAULT 0,
  position_x INTEGER NOT NULL DEFAULT ${DEFAULT_PLAYER_SPAWN_POSITION_X},
  position_y INTEGER NOT NULL DEFAULT ${DEFAULT_PLAYER_SPAWN_POSITION_Y},
  position_z INTEGER NOT NULL DEFAULT ${DEFAULT_PLAYER_SPAWN_POSITION_Z},
  capacity INTEGER NOT NULL DEFAULT 0,
  sex TINYINT NOT NULL DEFAULT ${DEFAULT_PLAYER_SEX},
  last_login UNSIGNED BIG INT NOT NULL DEFAULT 0,
  last_ip TEXT NOT NULL DEFAULT 0,
  skull TINYINT NOT NULL DEFAULT 0,
  skull_end_time UNSIGNED BIG INT NOT NULL DEFAULT 0,
  last_logout UNSIGNED BIG INT NOT NULL DEFAULT 0,
  online_time_minutes UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_fist INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_fist_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_club INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_club_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_sword INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_sword_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_axe INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_axe_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_dist INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_dist_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_shielding INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_shielding_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  skill_fishing INTEGER UNSIGNED NOT NULL DEFAULT 10,
  skill_fishing_tries UNSIGNED BIG INT NOT NULL DEFAULT 0,
  deleted TINYINT NOT NULL DEFAULT 0,
  UNIQUE(name),
  FOREIGN KEY(account_id) REFERENCES accounts(id) ON DELETE CASCADE,
  FOREIGN KEY(world_id) REFERENCES worlds(id) ON DELETE SET NULL)
  `
}